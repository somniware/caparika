// import { validationResult } from 'express-validator/check';
import { RequestHandler } from 'express';

// import User from '../models/user';

export const getOrders: RequestHandler = async (_req, _res, _next) => {
  
};